tosca_definitions_version: tosca_simple_yaml_1_3

imports:
  - sub/mini.yaml

artifact_types:
  tadej.artifacts.Zip:
    derived_from: tosca.artifacts.Root

node_types:
  my.node.type.A:
    derived_from: tosca.nodes.Root
    artifacts:
      dummy_a: artifacts/script_a.yaml
