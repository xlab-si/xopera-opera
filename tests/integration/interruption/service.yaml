tosca_definitions_version: tosca_simple_yaml_1_3

node_types:
  cli_commands_test.nodes.interrupted:
    derived_from: tosca.nodes.SoftwareComponent
    interfaces:
      Standard:
        operations:
          create: playbooks/create.yaml
          delete: playbooks/delete.yaml

topology_template:
  node_templates:
    interrupted:
      type: cli_commands_test.nodes.interrupted
